import type { IngestInput } from "@/lib/store";

/**
 * Demo/seed market rows — leeg gehouden.
 * Radar vult alleen via live sync (LinkedIn Jobs, Indeed, platforms, …).
 */
export const seedSignals: IngestInput[] = [];
